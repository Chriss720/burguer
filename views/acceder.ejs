<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Acceder - Me Lleva la Burger</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@400;700&display=swap" rel="stylesheet">
    <style> .font-oswald { font-family: 'Oswald', sans-serif; } </style>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>

    <%- include('partials/header') %>

    <main class="min-h-screen flex items-center justify-center bg-black text-white font-oswald p-8">
      <div class="w-full max-w-md bg-[#1a1a1a] p-8 rounded-xl border-2 border-[#FFC72C] text-center">
        <h1 class="text-2xl text-[#FFC72C] font-extrabold mb-6 uppercase">INICIA SESIÓN</h1>
        <form class="space-y-4 login-form">
          <div>
            <label for="email" class="block text-left mb-1">Correo Electrónico</label>
            <input type="email" id="email" name="email" required class="w-full p-3 bg-[#333] border border-[#555] rounded-md text-white" />
          </div>
          <div>
            <label for="password" class="block text-left mb-1">Contraseña</label>
            <input type="password" id="password" name="password" required class="w-full p-3 bg-[#333] border border-[#555] rounded-md text-white" />
          </div>
          <button type="submit" class="w-full p-3 bg-[#DA291C] text-white font-extrabold rounded-full uppercase hover:bg-[#a81f13]">Entrar</button>

          <p class="hidden text-red-400" id="error-msg">Comprueba tu contraseña y nombre de cuenta e inténtalo de nuevo.</p>
        </form>

        <div class="mt-4 text-sm text-gray-300">
          <p><a href="#" class="text-[#FFC72C]">¿Olvidaste tu contraseña?</a></p>
          <p class="mt-2">¿No tienes cuenta? <a href="/registro" class="text-[#FFC72C]">Regístrate aquí</a></p>
        </div>
      </div>
    </main>

    <%- include('partials/footer') %>

  <script>
    const form = document.querySelector('.login-form');
    const errorMsg = document.getElementById('error-msg');

    form.addEventListener('submit', async (e) => {
      e.preventDefault();

      const email = document.getElementById('email').value.trim();
      const password = document.getElementById('password').value.trim();

      if (!email || !password) {
        errorMsg.style.display = 'block';
        errorMsg.textContent = '⚠️ Por favor, completa todos los campos.';
        return;
      }

      try {
        const res = await fetch('http://localhost:3000/auth/login', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            email: email,
            contrasena: password,
          }),
        });

        const data = await res.json();

        if (!res.ok) {
          errorMsg.style.display = 'block';
          errorMsg.textContent =
            '❌ Comprueba tu contraseña y nombre de cuenta e inténtalo de nuevo.';
          return;
        }

        // Guardar token en localStorage Y en cookie para que se envíe automáticamente
        localStorage.setItem('token', data.access_token);
        localStorage.setItem('clienteActual', JSON.stringify(data.user));
        
        // Guardar en cookie para que se envíe en todas las peticiones
        document.cookie = `Authorization=Bearer ${data.access_token}; path=/; max-age=86400`;

        if (data.user.tipo === 'employee') {
          window.location.href = '/admin/dashboard';
        } else {
          window.location.href = '/';
        }
      } catch (error) {
        console.error('Error:', error);
        errorMsg.style.display = 'block';
        errorMsg.textContent =
          '⚠️ No se pudo conectar con el servidor. Inténtalo más tarde.';
      }
    });
  </script>
</body>
</html>