<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menú - Admin</title>
    <link href="https://fonts.googleapis.com/css2?family=Oswald:wght@400;700&display=swap" rel="stylesheet">
    <style> .font-oswald { font-family: 'Oswald', sans-serif; } </style>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-black text-white">

<%- include('./header') %>

        <div class="p-8">
            <div class="flex justify-between items-center mb-8">
                <h2 class="text-4xl font-extrabold text-[#FFC72C]">Gestión de Menú</h2>
                <button id="add-product-btn" class="bg-[#FFC72C] text-black px-6 py-2 rounded-lg font-bold hover:bg-[#FFB700]">+ Nuevo Producto</button>
            </div>

            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
                <% if (products && products.length > 0) { %>
                    <% products.forEach(product => { %>
                        <div class="bg-[#1a1a1a] rounded-lg overflow-hidden border-2 border-[#FFC72C]">
                            <img src="<%= product.foto %>" alt="<%= product.nombre_producto %>" class="w-full h-48 object-cover">
                            <div class="p-4">
                                <h3 class="text-xl font-bold text-[#FFC72C] mb-2"><%= product.nombre_producto %></h3>
                                <p class="text-sm text-gray-300 mb-3"><%= product.descripcion %></p>
                                <div class="flex justify-between items-center">
                                    <span class="text-lg font-extrabold text-white">$<%= product.precio.toFixed(2) %></span>
                                    <div class="space-x-2">
                                        <button class="bg-[#FFC72C] text-black px-3 py-1 rounded font-bold hover:bg-[#FFB700]">Editar</button>
                                        <button class="bg-[#DA291C] text-white px-3 py-1 rounded font-bold hover:bg-[#a81f13]">Eliminar</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    <% }); %>
                <% } else { %>
                    <div class="col-span-3 text-center py-16">
                        <p class="text-gray-400 text-lg">No hay productos en el menú</p>
                    </div>
                <% } %>
            </div>
        </div>

<%- include('./footer') %>

</body>
</html>
